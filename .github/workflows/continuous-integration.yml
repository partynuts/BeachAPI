name: Beach API CI
# This workflow is triggered on pushes to the repository.
on: [push]

jobs:
  container-jobs:
    runs-on: ubuntu-latest
    container: node:10.18-jessie

    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health_interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v2
      - name: install dependencies
        run: yarn
      - name: Execute tests
        run: yarn test
        env:
          DATABASE_URL: postgres://postgres:postgres@localhot:5432/postgres
